// Base authenticated layout — project pages bring their own AppNav via the project layout.
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
